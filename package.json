{
  "name": "scg-contabilidad",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de Contabilidad Ganadera — React + Node",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --include=dev && npm install --prefix server --include=dev && npm install --prefix client --include=dev",
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm run dev --prefix server\" \"wait-on -t 60000 http-get://127.0.0.1:3001/api/health && npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "build:render": "npm run install:all && npm run build",
    "build:vercel": "npm run install:all && npm run build",
    "start": "npm run start --prefix server"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^12.10.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.5"
  }
}
