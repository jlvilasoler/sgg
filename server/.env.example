NODE_ENV=production
PORT=3001
HOST=127.0.0.1
SCG_CLIENT_ORIGIN=https://sgg-dcpo.onrender.com
SCG_TRUST_PROXY=1
SCG_ADMIN_EMAIL=jlvilasoler@gmail.com
SCG_ADMIN_PASSWORD=CambiarPorUnaSegura!

# Vercel: SCG_CLIENT_ORIGIN es opcional (se detecta VERCEL_URL).
# SQLite en Vercel usa /tmp (datos efímeros). Para persistir: SCG_DATA_DIR en disco externo.
