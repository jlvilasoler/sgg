NODE_ENV=development
PORT=3001
HOST=127.0.0.1

# NO usar conexión directa db.xxx.supabase.co:5432 en Vercel (solo IPv6).
# Supabase → Database → Connection string → URI → Transaction pooler (6543):
DATABASE_URL=postgresql://postgres.mxcrpumaadtixlmnlgmj:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

SCG_CLIENT_ORIGIN=http://127.0.0.1:5173
SCG_TRUST_PROXY=0
SCG_ADMIN_EMAIL=jlvilasoler@gmail.com
SCG_ADMIN_PASSWORD=Admin123

# Vercel: configurar DATABASE_URL, SCG_ADMIN_EMAIL y SCG_ADMIN_PASSWORD en el dashboard.
# SCG_CLIENT_ORIGIN es opcional (se detecta VERCEL_URL automáticamente).
